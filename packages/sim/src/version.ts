/**
 * Stamped on every GradeResult. Bump whenever a coefficient in the shot
 * model, archetype table, terrain factors, or metric formulas changes —
 * the moment any of those change, every stored score becomes incomparable.
 */
export const SIM_VERSION = "0.1.0";
